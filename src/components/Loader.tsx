import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

export const Loader = () => {
  return (
    <View>
      <Text>Loading...</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
